//Assignment operators =
var age = 27;
var firstName = 'Vivek';
var money = 50.05;

//Arithmetic operators
age = age + 1;
var newAge = age + 1;
age += 1;
var fullName = firstName + ' Patel';

//comparision operators
var canAfford = money >= 20;

//logical operators
var cannotAfford = !canAfford;
var canBuy = canAfford && age >= 18;